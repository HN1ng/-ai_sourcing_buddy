# AI Sourcing Buddy · 使用说明

面向非技术岗位的招聘寻源工作台：陌生岗位也能照着走完"需求解析 → 渠道搜索 → 简历评估 → 排序分析"的完整链路。

## 一、如何打开

1. 用浏览器（推荐 Chrome / Edge / Safari）打开本文件夹中的 `index.html`；
2. 或双击 `index.html`，选择浏览器打开。

无需安装、无需联网、无需账号。所有数据只保存在你电脑的浏览器本地。

## 二、快速上手（一个需求的完整流程）

### 第 1 步：新建需求档案

右上角"需求档案"下拉框旁点"＋ 新建"，给需求起个名字（例如"企业服务销售经理"）；左侧竖排的三个入口（人才画像 / 搜索指令 / 候选人）切换功能模块。

### 第 2 步：解析 JD，确认人才画像

1. 切到「人才画像」页，把招聘 JD 粘贴进输入框，点"解析 JD"；
2. 系统自动提取：岗位名称（中英文）、岗位别名、必备/加分条件、经验区间、学历、行业、目标公司池、地点；
3. **逐项检查并补充**（尤其是必备条件；目标公司池需要手动填写，可以留空，留空时工具会跳过公司维度）；
4. 参考页面顶部的"建议先和用人经理确认"清单，把不清楚的点问清楚；
5. 点"保存画像"。

### 第 3 步：生成搜索指令

点"生成搜索指令 →"，工具会产出：

- **人才梯队搜索策略**（推荐主用）：四层漏斗，从精准到宽泛——
  - 第一梯队：对标公司 × 相同岗位（先搜，人少而准，转化率最高）
  - 第二梯队：相近行业 × 相同岗位（行业放宽一层，上手快）
  - 第三梯队：跨行业 × 岗位经验可复用（不限行业，只看岗位+能力）
  - 第四梯队：相似岗位 × 共同能力（换岗位叫法补 pipeline，需人工筛）
  - 每个梯队都给出中文组合（脉脉/猎聘/BOSS 用）和英文组合（LinkedIn/Google 用），可单独复制；
- **通用指令**：中文精准版 / 中文宽松版 / 英文精准版 / 英文宽松版；
- **渠道使用说明**：
  - 脉脉（深度）：关键词组合规则（引号=精准）+ 筛选器建议 + 使用要点 + 降级路径；
  - LinkedIn（深度）：免费版限制说明 + Google 绕过版指令；
  - 猎聘、BOSS直聘（基础）：筛选建议 + 降级路径；
- **渠道策略建议**：推荐的搜索顺序。

每条指令旁边都有"复制"按钮。历史生成记录会保留在页面下方，可随时回溯。

### 第 4 步：录入候选人并评分排序

1. 切到「候选人」页，点"＋ 添加候选人"；
2. 粘贴简历全文，或上传 PDF（自动解析）；
3. 点"解析简历"，检查自动提取的信息（姓名、职位、公司、年限、学历、行业、能力词），可手动修正；
4. 点"加入 Pipeline 并分析"——系统按六维模型评分并自动排序。

六维评分默认权重：职能匹配 25%、业绩成果 25%、行业经验 20%、公司背景 12%、经验年限 10%、学历 8%。可在候选人页调整权重，改完全部候选人自动重算。

### 第 5 步：查看分析报告

点击列表中任意候选人，打开完整报告：

- 六维评分（雷达图 + 明细条）
- 亮点清单（名企背景、量化业绩、行业匹配等）
- 顾虑清单（必备条件缺失、稳定性、学历、业绩无数字等）
- 待核实问题（联系前建议问清楚的事项）
- 沟通建议（优先沟通 / 可沟通 / 备选 / 暂缓）

列表支持：按维度排序、置顶、冻结（锁定位置）、移除（可在"显示已剔除"中找回）。

### 第 6 步：导出与备份

- "导出对比表 (CSV)"：生成候选人对比表，用 Excel 可直接打开；
- 侧边栏 / 设置中的"导出数据"：完整备份（JSON），换电脑或误删后可"导入数据"恢复；
- 设置中的"清空全部数据"：彻底清除本地数据。

## 三、常见问题

**AI 深度分析怎么用？**

设置中开启"AI 深度分析"并填写接口地址、API Key（OpenAI 兼容接口均可，如 OpenAI / DeepSeek / 本地 Ollama）。Key 只保存在本机浏览器，不会进入交付物；启用后 JD 和简历会发送到你填写的接口。使用者各自的 Key 各自填写，互不消耗。

开启后：「解析 JD」旁会出现"AI 深度解析"；候选人分析报告里会出现"AI 深度分析"按钮，输出语义级的亮点、顾虑、待核实问题和总结。未开启时，内置规则引擎照常工作，完全离线。

**PDF 上传解析失败？**

分两种情况：

1. **扫描件 / 图片型 PDF**（提示"没有可提取的文本"）：这类 PDF 整页是图片、没有文字层，任何解析工具都提取不到文字，只能 OCR 识别。当前请把简历的文字版复制粘贴进来；
2. **扫描件自动 OCR**：工具已内置 OCR 识别（支持中文），检测到扫描件会自动识别文字，然后照常解析、评分。首次识别需要联网下载识别模型（约几十 MB），需等待片刻；离线环境请粘贴文字版。
3. **本地文件模式限制**（提示具体加载/解析失败）：在"直接双击打开"的本地文件模式下，个别浏览器会限制解析资源加载。可改用粘贴文本，部署为网址后不受此限制。

**解析结果不准确？**

规则引擎的解析结果是"草稿"，所有字段都可以手动修改。建议重点人工确认：岗位别名、必备条件、目标公司池、经验区间。

**换了电脑 / 浏览器，数据还在吗？**

不在。数据存在当前电脑的浏览器里。跨设备使用前，请先在原设备"导出数据"，再到新设备"导入数据"。

**清除浏览器缓存会丢数据吗？**

可能。建议定期导出备份（JSON），重要数据不要只依赖浏览器存储。

## 四、使用建议

- **先澄清再搜索**：搜索指令和评分的质量取决于画像，花 5 分钟确认"硬性条件"和"目标公司池"，能省 1 小时筛选时间；
- **优先联系前 10 位**：工具会在候选人页提示综合分最高的前 3 位，建议先发触达；
- **能力推断**：工具会从职责表述推断隐含能力（如"全球增长"推断英语 / 获客、"ROI"推断成本控制）。推断项会计入匹配分，并标注在亮点与待核实问题中，联系前请核实实际水平；
- **脉脉搜人技巧**：把岗位名用双引号包起来（如 `"销售经理"`）可精准匹配完整词组；其余能力词不加引号、用空格组合。引号越多越精准、结果越少，搜不到时去掉引号放宽；
- **LinkedIn 免费账号限制**：单次搜索最多约 1000 条结果；搜索/浏览有月度额度，用于招聘时可能触发"商业用途限制"；没有 InMail、看不到非好友完整资料、筛选器只有基础几项。批量找简历需升级 Recruiter Lite / Sales Navigator，免费额度内建议配合 Google 绕过版指令使用；
- **业绩必须核实**：业绩成果占 25% 权重，联系时优先用"待核实问题"里的业绩口径问题把关；
- **搜索指令按渠道微调**：脉脉、LinkedIn 等平台的语法会更新，指令是模板建议，可自行调整后复制。

## 五、版本记录

- v1.0（2026-08-16）：首个版本。需求解析、四渠道搜索指令、六维评分、多需求档案、CSV / JSON 导出导入，纯本地离线运行。
